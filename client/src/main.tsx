import React from "react";
import ReactDOM from "react-dom/client";
import SparkFixApp from "./SparkFixApp";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <SparkFixApp />
  </React.StrictMode>
);
